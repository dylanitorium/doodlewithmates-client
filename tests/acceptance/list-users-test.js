import { test } from 'qunit';
import moduleForAcceptance from 'doodlewithmates/tests/helpers/module-for-acceptance';

moduleForAcceptance('Acceptance | list users');

test('should see user list', (accept) => {

});

test('should see chat screen', (accept) => {

});

test('should see canvas', (accept) => {

});
